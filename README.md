# CNN-for-brain-tumor

This notebook shows the development and testing of a custom convolutional neural network for multi-class medical image classification. It covers data loading, model training with tracking of loss and accuracy, and performance evaluation using metrics and visuals like confusion matrices. The model's strength is also tested on a separate dataset, showing encouraging results similar to DenseNet121.
